---
import Navbar from "../components/Navbar.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content=" I specialize in building web applications and sites using
    Javascript, React, Nextjs, Astro and Node. I have also dabbled with
    python at work." />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class="px-4 md:container md:mx-auto">
    <Navbar />
    <slot />
    <hr class="h-px w-full my-8 bg-gray-200 border-0 dark:bg-gray-700" />

    <footer class="text-center my-2">
      <p class="text-slate-500 font-sans text-xs">Shashank Bhat &copy; 2024</p>
    </footer>
    <script src="../js/main.js"></script>
   
  </body>
</html>

<style is:global>
  :root {
    --accent: 136, 58, 234;
    --accent-light: 224, 204, 250;
    --accent-dark: 49, 10, 101;
    --accent-gradient: linear-gradient(
      45deg,
      rgb(var(--accent)),
      rgb(var(--accent-light)) 30%,
      white 60%
    );
  }
  @font-face {
    font-family: "Krylon";
    src: url("/fonts/Krylon-Regular.woff") format("woff");
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: "Clinton";
    src: url("/fonts/Clinton.ttf") format("ttf");
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: "Clinton Bold";
    src: url("/fonts/clintonbolditalic.ttf") format("ttf");
    font-weight: bold;
    font-style: normal;
    font-display: swap;
  }
  html {
    font-family: system-ui, sans-serif;
    background: #101010;
    /* background: white; */
    background-size: 224px;
  }
  code {
    font-family:
      Menlo,
      Monaco,
      Lucida Console,
      Liberation Mono,
      DejaVu Sans Mono,
      Bitstream Vera Sans Mono,
      Courier New,
      monospace;
  }
</style>
